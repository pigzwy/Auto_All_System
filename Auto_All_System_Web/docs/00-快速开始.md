# 快速开始指南

## 🚀 5分钟快速启动

### 前置要求

- ✅ **Docker Desktop** (推荐)
- ✅ **Git** 2.x
- ✅ **Windows PowerShell** / macOS Terminal / Linux Shell

---

## 📥 Step 1: 克隆项目

```bash
git clone <repository-url>
cd Auto_All_System
```

---

## 🏃 Step 2: 启动系统

### Windows PowerShell

```powershell
# 启动所有服务
docker-compose up -d --build

# 查看服务状态
docker-compose ps
```

### Mac / Linux

```bash
# 启动所有服务
docker-compose up -d --build

# 查看服务状态
docker-compose ps
```

**等待片刻，直到所有服务状态显示为 `Up`**

---

## 🗄️ Step 3: 初始化数据库

```bash
# 创建数据库表
docker-compose exec backend python manage.py migrate

# 创建超级管理员账号
docker-compose exec backend python manage.py createsuperuser
# 按提示输入用户名、邮箱、密码
```

**推荐账号**:
- 用户名: `admin`
- 邮箱: `admin@example.com`
- 密码: `admin123`

---

## 🌐 Step 4: 访问系统

### 打开浏览器访问

| 服务 | 地址 |
|------|------|
| **前端应用** | http://localhost |
| **管理后台** | http://localhost/admin |
| **API文档** | http://localhost:8000/api/docs/ |
| **健康检查** | http://localhost:8000/api/health/ |

### 登录系统

使用刚才创建的管理员账号登录

---

## ✅ Step 5: 验证安装

### 1. 检查健康状态

```bash
curl http://localhost:8000/api/health/
```

**预期响应**:
```json
{
  "status": "healthy",
  "timestamp": "2026-01-16T12:00:00Z",
  "database": "connected",
  "cache": "connected"
}
```

### 2. 查看服务日志

```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f backend
docker-compose logs -f frontend
```

### 3. 测试功能

1. ✅ 登录系统
2. ✅ 查看首页仪表盘
3. ✅ 浏览专区列表
4. ✅ 查看任务列表
5. ✅ 访问管理后台

---

## 🛑 停止系统

```bash
# 停止所有服务
docker-compose down

# 停止并删除数据卷（会清空数据库）
docker-compose down -v
```

---

## 🔄 重启系统

```bash
# 重启所有服务
docker-compose restart

# 重启特定服务
docker-compose restart backend
docker-compose restart frontend
```

---

## 🔧 常见问题

### Q1: 端口被占用

**错误**: `Bind for 0.0.0.0:80 failed: port is already allocated`

**解决方案**: 修改 `docker-compose.yml` 的端口映射

```yaml
frontend:
  ports:
    - "8080:80"  # 改为8080端口
```

然后访问 http://localhost:8080

### Q2: 数据库连接失败

**错误**: `could not connect to server`

**解决方案**:
```bash
# 检查数据库服务状态
docker-compose logs db

# 重启数据库服务
docker-compose restart db

# 等待几秒后重试
```

### Q3: 前端页面无法访问

**解决方案**:
```bash
# 清除浏览器缓存
# 按 Ctrl + Shift + Delete (Windows)
# 或 Cmd + Shift + Delete (Mac)

# 或使用无痕模式访问
# 按 Ctrl + Shift + N (Windows)
# 或 Cmd + Shift + N (Mac)

# 重新构建前端
docker-compose up -d --build frontend
```

### Q4: 看不到新功能/新页面

**解决方案**:
```bash
# 强制刷新浏览器
# 按 Ctrl + F5 (Windows)
# 或 Cmd + Shift + R (Mac)

# 或清除浏览器缓存后重新访问
```

### Q5: Redis连接失败

**解决方案**:
```bash
# 测试Redis连接
docker-compose exec redis redis-cli ping
# 应该返回: PONG

# 如果失败，重启Redis
docker-compose restart redis
```

---

## 🎯 Google 插件快速使用

### 1. 启用 Google 插件

```bash
# 运行数据库迁移
docker-compose exec backend python manage.py migrate google_business

# 安装依赖（如果需要）
docker-compose exec backend pip install playwright pyotp deep-translator
docker-compose exec backend python -m playwright install chromium
```

### 2. 访问 Google 插件

浏览器访问: http://localhost/google/dashboard

### 3. 导入账号

1. 访问 **账号管理** 页面
2. 点击 **批量导入**
3. 格式：`email----password----recovery_email----secret_key`
4. 点击 **导入**

### 4. 一键全自动处理

1. 访问 **一键全自动处理** 页面
2. 输入 SheerID API Key
3. 上传卡片文件 (cards.txt)
4. 选择要处理的账号
5. 点击 **开始全自动处理**

**完整流程**:  
登录 → 检测资格 → SheerID验证 → 绑卡 → 订阅

---

## 📚 下一步

### 开发者

- 阅读 [02-系统架构与配置.md](./02-系统架构与配置.md)
- 阅读 [01-数据库设计文档.md](./01-数据库设计文档.md)
- 阅读 [18-Google插件迁移指南.md](./18-Google插件迁移指南.md)
- 查看 API 文档: http://localhost:8000/api/docs/

### 用户

- 阅读 [03-前端页面功能说明.md](./03-前端页面功能说明.md)
- 体验 Google 插件功能
- 体验新功能: 账户充值、VIP会员

### 管理员

- 查看管理后台功能
- 配置系统设置
- 管理用户和订单

---

## 📋 快速命令参考

### Docker命令

```bash
# 启动
docker-compose up -d

# 停止
docker-compose down

# 重启
docker-compose restart

# 查看状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 进入容器
docker-compose exec backend bash
docker-compose exec db psql -U auto_all_user -d auto_all_db
```

### Django命令

```bash
# 数据库迁移
docker-compose exec backend python manage.py migrate

# 创建超级用户
docker-compose exec backend python manage.py createsuperuser

# Django Shell
docker-compose exec backend python manage.py shell

# 收集静态文件
docker-compose exec backend python manage.py collectstatic
```

### 数据库命令

```bash
# 备份数据库
docker-compose exec db pg_dump -U auto_all_user auto_all_db > backup.sql

# 恢复数据库
docker-compose exec -T db psql -U auto_all_user auto_all_db < backup.sql

# 进入PostgreSQL
docker-compose exec db psql -U auto_all_user -d auto_all_db
```

---

## 🎯 测试数据

### 创建测试数据

```bash
# 进入Django Shell
docker-compose exec backend python manage.py shell

# 执行以下Python代码
from apps.accounts.models import User
from apps.zones.models import Zone
from apps.tasks.models import Task

# 创建测试用户
user = User.objects.create_user(
    username='testuser',
    email='test@example.com',
    password='test123'
)

# 创建测试专区
zone = Zone.objects.create(
    name='测试专区',
    code='test_zone',
    description='这是一个测试专区',
    plugin_class='apps.zones.plugins.TestPlugin',
    is_active=True,
    price_per_task=10.00
)

print("测试数据创建成功！")
```

---

## 💡 提示

### 开发模式

- **热重载**: 前端代码修改会自动重新加载
- **调试模式**: 后端开启DEBUG模式，显示详细错误信息
- **API文档**: 自动生成，访问 http://localhost:8000/api/docs/

### 生产模式

- **HTTPS**: 参考 [02-系统架构与配置.md](./02-系统架构与配置.md) 启用HTTPS
- **安全设置**: 修改默认密码，设置强密钥
- **性能优化**: 启用缓存，优化数据库查询

---

## 🆘 获取帮助

### 文档

- [00-快速开始.md](./00-快速开始.md) (本文档)
- [01-数据库设计文档.md](./01-数据库设计文档.md)
- [02-系统架构与配置.md](./02-系统架构与配置.md)
- [03-前端页面功能说明.md](./03-前端页面功能说明.md)

### 日志

```bash
# 查看详细日志
docker-compose logs -f

# 查看错误日志
docker-compose logs -f | grep ERROR
```

### 健康检查

```bash
curl http://localhost:8000/api/health/
```

---

**🎉 恭喜！系统已成功启动！**

现在可以开始使用 Auto All System 了！

**祝使用愉快！** 🚀

---

**文档版本**: 1.0.0  
**最后更新**: 2026-01-16  
**维护者**: Auto All System Team

