# 新增专区开发指南

本文档详细说明如何在系统中新增一个业务专区，以及如何在现有专区中添加功能模块。

---

## 目录

- [概述](#概述)
- [专区架构说明](#专区架构说明)
- [Part A: 新增专区](#part-a-新增专区)
- [Part B: 在现有专区中添加功能模块](#part-b-在现有专区中添加功能模块)
- [文件模板](#文件模板)
- [注意事项](#注意事项)

---

## 概述

### 什么是专区？

专区是一个独立的业务功能模块，拥有：
- 独立的页面入口
- 独立的路由配置
- 独立的子模块组件
- 独立的侧边导航菜单

### 现有专区参考

| 专区名称 | 路由路径 | 主页面组件 | 模块目录 |
|----------|----------|------------|----------|
| Google 业务专区 | `/google-zone` | `GoogleBusinessZone.vue` | `google-modules/` |
| GPT 业务专区 | `/gpt-zone` | `GptBusinessZone.vue` | `gpt-modules/` |

---

## 专区架构说明

### 目录结构

```
frontend/src/
├── router/
│   ├── index.ts                    # 主路由（引入模块路由）
│   └── modules/
│       ├── google.ts               # Google 专区路由模块
│       └── gpt.ts                  # [新增] GPT 专区路由模块
│
├── views/
│   └── zones/
│       ├── ZoneListView.vue        # 专区列表页（入口卡片）
│       ├── GoogleBusinessZone.vue  # Google 专区主页面
│       ├── GptBusinessZone.vue     # [新增] GPT 专区主页面
│       │
│       ├── google-modules/         # Google 专区子模块
│       │   ├── WorkstationModule.vue
│       │   ├── GoogleAccountsModule.vue
│       │   └── ...
│       │
│       └── gpt-modules/            # [新增] GPT 专区子模块
│           ├── WorkstationModule.vue
│           └── ...
```

### 专区组成要素

```
┌─────────────────────────────────────────────────────────────┐
│                        专区主页面                            │
│  ┌─────────────┬───────────────────────────────────────┐   │
│  │   侧边栏     │              内容区域                  │   │
│  │   ────────  │   ┌─────────────────────────────┐     │   │
│  │   工作台     │   │                             │     │   │
│  │   账号管理   │   │     当前选中的子模块组件      │     │   │
│  │   设置      │   │                             │     │   │
│  │   ...       │   └─────────────────────────────┘     │   │
│  └─────────────┴───────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## Part A: 新增专区

本节说明如何从零创建一个全新的业务专区（以 GPT 专区为例）。

### 步骤总览

| 步骤 | 操作类型 | 文件路径 | 说明 |
|------|----------|----------|------|
| 1 | 新建 | `src/router/modules/gpt.ts` | 专区路由模块（可选：模块化维护） |
| 2 | 修改 | `src/router/index.ts` | 注册专区路由（当前项目实际采用“直接写路由”方式） |
| 3 | 新建 | `src/views/zones/gpt-modules/` | 子模块目录 |
| 4 | 新建/修改 | `src/views/zones/gpt-modules/WorkstationModule.vue` | 工作台模块（推荐承载“配置 + 任务”，配置走 Web，不使用本地配置文件） |
| 5 | 新建/修改 | `src/views/zones/GptBusinessZone.vue` | 专区主页面（独立布局 + 侧边菜单） |
| 6 | 修改 | `src/views/zones/ZoneListView.vue` | 添加入口卡片 |
| 7 | （可选/推荐）新建 | `backend/plugins/<your_plugin>/` | 后端插件提供 API（适合需要任务/导出等能力的专区） |

---

### 步骤 1：创建路由模块

**文件：** `src/router/modules/gpt.ts`

```typescript
/**
 * GPT 业务专区路由配置（预留）
 *
 * 说明：当前项目的主路由在 `src/router/index.ts` 里直接维护。
 * 这里保留模块化定义，便于后续统一管理。
 */
import type { RouteRecordRaw } from 'vue-router'

export const gptRoutes: Array<RouteRecordRaw> = [
  {
    path: '/gpt-zone',
    name: 'GptBusinessZone',
    component: () => import('@/views/zones/GptBusinessZone.vue'),
    meta: {
      title: 'GPT 业务',
      requiresAuth: true
    }
  }
]
```

---

### 步骤 2：引入路由模块

**文件：** `src/router/index.ts`

当前项目实践：主路由直接维护在 `src/router/index.ts`，新增专区时**直接新增一条 route 即可**（无需 import）。

在路由数组中添加（紧跟 Google 专区路由后面）：

```typescript
// Google 业务专区 - 完全独立的页面
{
  path: '/google-zone',
  name: 'GoogleBusinessZone',
  component: () => import('@/views/zones/GoogleBusinessZone.vue'),
  meta: { requiresAuth: true }
},
// GPT 业务专区 - 完全独立的页面
{
  path: '/gpt-zone',
  name: 'GptBusinessZone',
  component: () => import('@/views/zones/GptBusinessZone.vue'),
  meta: { requiresAuth: true }
}
```

> 可选：如果你希望降低 `router/index.ts` 合并冲突，也可以采用模块化路由。
> 
> 1) 顶部导入：`import { gptRoutes } from './modules/gpt'`
> 
> 2) 在 routes 里追加：`...gptRoutes`

---

### 步骤 3：创建子模块目录

创建目录：`src/views/zones/gpt-modules/`

---

### 步骤 4：创建工作台模块

**文件：** `src/views/zones/gpt-modules/WorkstationModule.vue`

> 提示：GPT 专区在当前系统里已经将“配置 + 任务创建 + 产物下载”集成在 `WorkstationModule.vue` 中，配置全部来自 Web（后端 `PluginState.settings`），不再使用本地 `config.toml/team.json` 作为用户配置入口。
> 
> 下方代码仅作为“专区工作台结构示例”。若你是新增 GPT 专区，建议直接参考现有实现：`frontend/src/views/zones/gpt-modules/WorkstationModule.vue`。

```vue
<template>
  <div class="workstation-module">
    <el-row :gutter="20">
      <!-- 统计卡片 -->
      <el-col :xs="24" :sm="12" :md="6">
        <el-card class="stat-card" shadow="hover">
          <div class="stat-icon" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
            <el-icon :size="28"><User /></el-icon>
          </div>
          <div class="stat-info">
            <div class="stat-value">{{ stats.totalAccounts }}</div>
            <div class="stat-label">总账号数</div>
          </div>
        </el-card>
      </el-col>
      
      <el-col :xs="24" :sm="12" :md="6">
        <el-card class="stat-card" shadow="hover">
          <div class="stat-icon" style="background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);">
            <el-icon :size="28"><Check /></el-icon>
          </div>
          <div class="stat-info">
            <div class="stat-value">{{ stats.activeAccounts }}</div>
            <div class="stat-label">活跃账号</div>
          </div>
        </el-card>
      </el-col>
      
      <el-col :xs="24" :sm="12" :md="6">
        <el-card class="stat-card" shadow="hover">
          <div class="stat-icon" style="background: linear-gradient(135deg, #fc4a1a 0%, #f7b733 100%);">
            <el-icon :size="28"><Clock /></el-icon>
          </div>
          <div class="stat-info">
            <div class="stat-value">{{ stats.pendingTasks }}</div>
            <div class="stat-label">待处理任务</div>
          </div>
        </el-card>
      </el-col>
      
      <el-col :xs="24" :sm="12" :md="6">
        <el-card class="stat-card" shadow="hover">
          <div class="stat-icon" style="background: linear-gradient(135deg, #ee0979 0%, #ff6a00 100%);">
            <el-icon :size="28"><TrendCharts /></el-icon>
          </div>
          <div class="stat-info">
            <div class="stat-value">{{ stats.todayCompleted }}</div>
            <div class="stat-label">今日完成</div>
          </div>
        </el-card>
      </el-col>
    </el-row>

    <!-- 快捷操作 -->
    <el-card class="quick-actions" shadow="never" style="margin-top: 20px;">
      <template #header>
        <span>快捷操作</span>
      </template>
      <el-row :gutter="16">
        <el-col :xs="12" :sm="6">
          <el-button type="primary" size="large" style="width: 100%;">
            <el-icon><Plus /></el-icon>
            添加账号
          </el-button>
        </el-col>
        <el-col :xs="12" :sm="6">
          <el-button type="success" size="large" style="width: 100%;">
            <el-icon><Refresh /></el-icon>
            批量刷新
          </el-button>
        </el-col>
        <el-col :xs="12" :sm="6">
          <el-button type="warning" size="large" style="width: 100%;">
            <el-icon><Download /></el-icon>
            导出数据
          </el-button>
        </el-col>
        <el-col :xs="12" :sm="6">
          <el-button type="info" size="large" style="width: 100%;">
            <el-icon><Setting /></el-icon>
            系统设置
          </el-button>
        </el-col>
      </el-row>
    </el-card>

    <!-- 欢迎信息 -->
    <el-card shadow="never" style="margin-top: 20px;">
      <el-empty description="GPT 专区功能开发中，敬请期待..." />
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { reactive } from 'vue'
import { User, Check, Clock, TrendCharts, Plus, Refresh, Download, Setting } from '@/icons'

// 统计数据
const stats = reactive({
  totalAccounts: 0,
  activeAccounts: 0,
  pendingTasks: 0,
  todayCompleted: 0
})
</script>

<style scoped lang="scss">
.workstation-module {
  .stat-card {
    display: flex;
    align-items: center;
    padding: 20px;
    margin-bottom: 20px;
    
    .stat-icon {
      width: 60px;
      height: 60px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      margin-right: 16px;
    }
    
    .stat-info {
      .stat-value {
        font-size: 28px;
        font-weight: 600;
        color: #303133;
        line-height: 1;
        margin-bottom: 8px;
      }
      
      .stat-label {
        font-size: 14px;
        color: #909399;
      }
    }
  }
  
  .quick-actions {
    :deep(.el-card__body) {
      padding-top: 0;
    }
    
    .el-button {
      height: 50px;
      margin-bottom: 10px;
    }
  }
}
</style>
```

---

### 步骤 5：创建专区主页面

**文件：** `src/views/zones/GptBusinessZone.vue`

> 提示：下方代码是“专区主页面结构示例”。实际项目中的 GPT 专区主页面实现以 `frontend/src/views/zones/GptBusinessZone.vue` 为准（可能更简洁）。

```vue
<template>
  <div class="gpt-business-zone">
    <!-- 顶部触发区域和导航栏 -->
    <div class="navbar-wrapper">
      <!-- 触发区域 - 鼠标移到这里时显示导航栏 -->
      <div class="navbar-trigger"></div>
      <!-- 导航栏 -->
      <div class="top-navbar">
        <div class="navbar-content">
          <div class="logo-section">
            <el-icon class="menu-toggle" @click="toggleSidebar" :size="24">
              <Fold v-if="!sidebarCollapsed" />
              <Expand v-else />
            </el-icon>
            <el-icon class="logo-icon" :size="32"><ChatDotRound /></el-icon>
            <div class="logo-text">
              <h2>GPT 业务专区</h2>
              <span>GPT 相关自动化处理平台</span>
            </div>
          </div>
          <div class="navbar-actions">
            <el-button @click="goBack" type="default">
              <el-icon><Back /></el-icon>
              返回首页
            </el-button>
            <el-dropdown @command="handleCommand">
              <el-button type="primary">
                <el-icon><User /></el-icon>
                {{ userStore.user?.username || 'User' }}
                <el-icon class="el-icon--right"><ArrowDown /></el-icon>
              </el-button>
              <template #dropdown>
                <el-dropdown-menu>
                  <el-dropdown-item command="profile">个人信息</el-dropdown-item>
                  <el-dropdown-item divided command="logout">退出登录</el-dropdown-item>
                </el-dropdown-menu>
              </template>
            </el-dropdown>
          </div>
        </div>
      </div>
    </div>

    <!-- 主容器 -->
    <div class="main-container">
      <!-- 左侧导航栏 -->
      <div class="sidebar" :class="{ collapsed: sidebarCollapsed }">
        <el-menu
          :default-active="activeModule"
          class="sidebar-menu"
          :collapse="sidebarCollapsed"
          @select="handleModuleChange"
        >
          <el-menu-item index="workstation">
            <el-icon><DataLine /></el-icon>
            <template #title>工作台</template>
          </el-menu-item>
          <!-- 在这里添加更多菜单项 -->
          <!--
          <el-menu-item index="accounts">
            <el-icon><Avatar /></el-icon>
            <template #title>账号管理</template>
          </el-menu-item>
          -->
        </el-menu>
      </div>

      <!-- 右侧内容区域 -->
      <div class="content-area" :class="{ expanded: sidebarCollapsed }">
        <!-- 动态加载模块组件 -->
        <component :is="currentModuleComponent" />
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, shallowRef } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '@/stores/user'
import {
  ChatDotRound, User, ArrowDown, DataLine,
  Fold, Expand, Back
} from '@/icons'

// 导入模块组件
import WorkstationModule from './gpt-modules/WorkstationModule.vue'

const router = useRouter()
const userStore = useUserStore()

// 侧边栏状态
const sidebarCollapsed = ref(false)
const activeModule = ref('workstation')

// 模块组件映射
const moduleComponents: Record<string, any> = {
  workstation: WorkstationModule,
  // 添加更多模块映射
}

// 当前模块组件
const currentModuleComponent = shallowRef(WorkstationModule)

// 切换侧边栏
const toggleSidebar = () => {
  sidebarCollapsed.value = !sidebarCollapsed.value
}

// 切换模块
const handleModuleChange = (index: string) => {
  activeModule.value = index
  currentModuleComponent.value = moduleComponents[index] || WorkstationModule
}

// 返回首页
const goBack = () => {
  router.push('/zones')
}

// 处理用户菜单命令
const handleCommand = (command: string) => {
  switch (command) {
    case 'profile':
      router.push('/profile')
      break
    case 'logout':
      userStore.logout()
      router.push('/login')
      break
  }
}
</script>

<style scoped lang="scss">
.gpt-business-zone {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background: #f5f7fa;
  
  .navbar-wrapper {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    
    .navbar-trigger {
      height: 6px;
      background: transparent;
      position: relative;
      z-index: 101;
      pointer-events: auto;
    }
    
    .top-navbar {
      background: white;
      box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      transform: translateY(-100%);
      transition: transform 0.3s ease-in-out;
      z-index: 100;
      pointer-events: auto;
    }
    
    .navbar-trigger:hover ~ .top-navbar,
    .top-navbar:hover {
      transform: translateY(0);
    }
    
    .navbar-content {
      padding: 16px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      
      .logo-section {
        display: flex;
        align-items: center;
        gap: 16px;
        
        .menu-toggle {
          cursor: pointer;
          color: #606266;
          transition: color 0.3s;
          
          &:hover {
            color: #409eff;
          }
        }
        
        .logo-icon {
          color: #10a37f; // GPT 绿色
        }
        
        .logo-text {
          h2 {
            margin: 0;
            font-size: 20px;
            font-weight: 600;
            color: #303133;
          }
          
          span {
            font-size: 12px;
            color: #909399;
          }
        }
      }
      
      .navbar-actions {
        display: flex;
        align-items: center;
        gap: 12px;
      }
    }
  }
  
  .main-container {
    flex: 1;
    display: flex;
    overflow: hidden;
    margin-top: 0;
    
    .sidebar {
      width: 200px;
      background: white;
      box-shadow: 2px 0 8px rgba(0, 0, 0, 0.1);
      transition: width 0.3s;
      overflow: hidden;
      
      &.collapsed {
        width: 64px;
      }
      
      .sidebar-menu {
        border-right: none;
        height: 100%;
        
        :deep(.el-menu-item) {
          height: 56px;
          line-height: 56px;
          
          &.is-active {
            background: #ecf5ff;
            color: #409eff;
            border-right: 3px solid #409eff;
          }
        }
      }
    }
    
    .content-area {
      flex: 1;
      padding: 24px;
      overflow-y: auto;
      background: #f5f7fa;
      width: calc(100% - 200px);
      transition: width 0.3s;
      
      &.expanded {
        width: calc(100% - 64px);
      }
    }
  }
}

@media (max-width: 768px) {
  .gpt-business-zone {
    .main-container {
      .sidebar {
        position: fixed;
        left: 0;
        top: 64px;
        bottom: 0;
        z-index: 999;
        
        &.collapsed {
          transform: translateX(-100%);
        }
      }
      
      .content-area {
        width: 100% !important;
      }
    }
  }
}
</style>
```

---

### 步骤 6：添加入口卡片

**文件：** `src/views/zones/ZoneListView.vue`

在 `featured-section` 区域内（约第 33 行后），添加新专区卡片：

```vue
<!-- GPT 业务专区 -->
<el-col :xs="24" :sm="12" :md="8" :lg="6">
  <el-card class="zone-card featured-card gpt-featured-card" shadow="hover" @click="openGptZone">
    <el-tag type="warning" class="hot-tag">NEW</el-tag>
    <div class="zone-icon">
      🤖
    </div>
    <h3>GPT 业务</h3>
    <p class="zone-desc">OpenAI Team 批量开通/授权自动化</p>
    <div class="zone-stats">
      <div class="stat-item">
        <div class="stat-value">{{ gptStats.teams }}</div>
        <div class="stat-label">团队数</div>
      </div>
      <div class="stat-item">
        <div class="stat-value">{{ gptStats.accounts }}</div>
        <div class="stat-label">账号数</div>
      </div>
    </div>
    <div class="zone-footer">
      <el-tag size="small" type="success">自动化</el-tag>
      <span class="price">进入</span>
    </div>
  </el-card>
</el-col>
```

在 `<script setup>` 中添加：

```typescript
import { gptBusinessApi } from '@/api/gpt_business'

// GPT 专区统计
const gptStats = reactive({
  teams: 0,
  accounts: 0
})

const fetchGptStats = async () => {
  try {
    const stats = await gptBusinessApi.getStatistics()
    gptStats.teams = stats.teams || 0
    gptStats.accounts = stats.accounts || 0
  } catch (error) {
    // 插件未启用/未安装时可能返回 404
    gptStats.teams = 0
    gptStats.accounts = 0
    console.error('Failed to fetch GPT stats:', error)
  }
}

// 打开 GPT 专区
const openGptZone = () => {
  router.push('/gpt-zone')
}

onMounted(() => {
  fetchGptStats()
})
```

---

### 步骤 7（可选/推荐）：新增后端插件（GPT 专区实践）

如果你的专区不仅是“展示页面”，还需要 **任务队列 / 导出文件 / 运行自动化 / 权限控制** 等能力，推荐走当前系统的插件体系：`backend/plugins/<plugin_name>/`。

以 GPT 专区为例：我们新增了 `gpt_business` 插件，提供以下能力：

- Web 配置：所有参数写入后端 `PluginState.settings`，**不再让用户维护本地 `config.toml/team.json`**
- 任务流：
  - `invite_only`：仅 GPTMail 生成邮箱 + OpenAI Team 批量邀请
  - `legacy_run`：直接执行 legacy 仓库（`oai-team-auto-provisioner`，DrissionPage 自动化）
- 产物下载：`accounts.csv / team_tracker.json / run.log ...` 存储在 `MEDIA_ROOT/gpt_business/jobs/<task_id>/`，并通过 API 下载

关键代码路径：

- `backend/plugins/gpt_business/plugin.py`
- `backend/plugins/gpt_business/urls.py`
- `backend/plugins/gpt_business/views.py`
- `backend/plugins/gpt_business/tasks.py`
- `backend/plugins/gpt_business/legacy_runner.py`

> 说明：为了保持 legacy 仓库“零改动”，后端在执行 `legacy_run` 时会**内部生成**每个任务自己的 `config.toml` 与 `team.json`（仅用于执行兼容，不作为用户配置入口）。

### 启动脚本（Windows / Linux）

为了让新依赖（例如 DrissionPage）可以自动安装，启动脚本建议统一使用 `--build`：

- Windows：
  - `Auto_All_System_Web/一键启动.bat`
  - `Auto_All_System_Web/开发模式启动.bat`
- Linux（Ubuntu）：
  - `Auto_All_System_Web/一键启动.sh`
  - `Auto_All_System_Web/开发模式启动.sh`

Linux 脚本会自动设置 `GEEKEZ_BROWSER_PROFILES_PATH`（避免 `docker-compose.yml` 中的 Windows 路径挂载导致启动失败）。

## Part B: 在现有专区中添加功能模块

本节说明如何在已有的专区（如 Google 专区）中添加新的功能模块。

### 操作概述

在现有专区中添加功能模块非常简单，只需要 **2 步操作**：

| 步骤 | 操作类型 | 文件路径 | 说明 |
|------|----------|----------|------|
| 1 | 新建 | `src/views/zones/xxx-modules/NewModule.vue` | 新功能模块组件 |
| 2 | 修改 | `src/views/zones/XxxBusinessZone.vue` | 注册模块（3处修改） |

### 详细步骤（以 Google 专区添加"批量注册"功能为例）

#### 步骤 1：创建功能模块组件

**文件：** `src/views/zones/google-modules/BatchRegisterModule.vue`

```vue
<template>
  <div class="batch-register-module">
    <el-card>
      <template #header>
        <span>批量注册</span>
      </template>
      <!-- 你的功能实现 -->
      <el-empty description="批量注册功能开发中..." />
    </el-card>
  </div>
</template>

<script setup lang="ts">
// 你的业务逻辑
</script>

<style scoped lang="scss">
.batch-register-module {
  // 你的样式
}
</style>
```

#### 步骤 2：在专区主页面注册模块

**文件：** `src/views/zones/GoogleBusinessZone.vue`

需要修改 **3 处位置**：

##### 位置 A - 导入组件（约第 116-124 行）

```typescript
// 导入模块组件
import WorkstationModule from './google-modules/WorkstationModule.vue'
import GoogleAccountsModule from './google-modules/GoogleAccountsModule.vue'
import VirtualCardsModule from './google-modules/VirtualCardsModule.vue'
// ... 其他已有模块
import BatchRegisterModule from './google-modules/BatchRegisterModule.vue'  // [新增]
```

##### 位置 B - 注册到模块映射（约第 149-159 行）

```typescript
// 模块组件映射
const moduleComponents: Record<string, any> = {
  workstation: WorkstationModule,
  accounts: GoogleAccountsModule,
  cards: VirtualCardsModule,
  // ... 其他已有模块
  'batch-register': BatchRegisterModule  // [新增]
}
```

##### 位置 C - 添加侧边栏菜单项（约第 56-91 行）

```vue
<el-menu-item index="subscription">
  <el-icon><Tickets /></el-icon>
  <template #title>订阅验证</template>
</el-menu-item>

<!-- [新增] 批量注册菜单项 -->
<el-menu-item index="batch-register">
  <el-icon><DocumentAdd /></el-icon>
  <template #title>批量注册</template>
</el-menu-item>
```

> **注意**：如果使用了新图标（如 `DocumentAdd`），需要在 `<script>` 的 import 部分添加图标导入。

### 添加模块快速检查清单

- [ ] 创建模块组件文件 `xxx-modules/NewModule.vue`
- [ ] 在专区主页面导入组件
- [ ] 在 `moduleComponents` 映射中注册
- [ ] 在侧边栏菜单添加菜单项
- [ ] （可选）导入新图标

### 合并冲突风险

| 操作 | 冲突风险 | 说明 |
|------|----------|------|
| 新建模块组件 | ✅ 无风险 | 新文件，不会冲突 |
| 修改专区主页面 | ⚠️ 低风险 | 只修改一个文件，合并简单 |

---

## 注意事项

### 1. 代码合并冲突

| 文件类型 | 冲突风险 | 处理方式 |
|----------|----------|----------|
| 新建文件 | ✅ 无风险 | 直接创建 |
| `router/index.ts` | ⚠️ 中等 | 使用模块化路由降低风险 |
| `ZoneListView.vue` | ⚠️ 中等 | 合并时保留双方代码 |

### 2. 命名规范

- **路由路径**：`/xxx-zone`（小写，连字符分隔）
- **组件名称**：`XxxBusinessZone.vue`（大驼峰）
- **模块目录**：`xxx-modules/`（小写，连字符分隔）
- **路由模块**：`xxx.ts`（小写）

### 3. 后续扩展

添加新的子模块时：

1. 在 `xxx-modules/` 目录创建新组件
2. 在主页面的 `moduleComponents` 映射中注册
3. 在侧边栏菜单中添加菜单项

### 4. 后端 API

如果专区需要后端支持，需要：

1. 创建 Django App 或插件
2. 添加数据模型
3. 实现 API 接口
4. 前端添加对应的 API 调用模块（`src/api/xxx.ts`）

---

## 快速检查清单

创建新专区时，确认以下文件已完成：

- [ ] `src/router/modules/xxx.ts` - 路由模块
- [ ] `src/router/index.ts` - 引入路由
- [ ] `src/views/zones/xxx-modules/` - 模块目录
- [ ] `src/views/zones/xxx-modules/WorkstationModule.vue` - 工作台模块
- [ ] `src/views/zones/XxxBusinessZone.vue` - 专区主页面
- [ ] `src/views/zones/ZoneListView.vue` - 入口卡片

---

## 参考文件

- Google 专区主页面：`src/views/zones/GoogleBusinessZone.vue`
- Google 模块目录：`src/views/zones/google-modules/`
- Google 路由模块：`src/router/modules/google.ts`
